{
  "name": "@scrobble-bridge/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "format": "prettier --check --ignore-path ../../.prettierignore ."
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/chrome": "^0.1.36",
    "prettier": "^3.6.2",
    "typescript": "^6.0.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
