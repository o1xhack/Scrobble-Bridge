{
  "name": "@scrobble-bridge/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "package:store": "pnpm build && node scripts/package-store.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "format": "prettier --check --ignore-path ../../.prettierignore ."
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "@types/node": "^24.10.1",
    "prettier": "^3.6.2",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
