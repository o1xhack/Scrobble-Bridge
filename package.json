{
  "name": "scrobble-bridge",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "overrides": {
      "cookie": "^0.7.2"
    }
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm docs:check && pnpm -r check",
    "docs:check": "node scripts/check-docs.mjs",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  }
}
